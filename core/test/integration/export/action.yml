name: 'Core Export Testing'
description: 'A Github Action used for testing exports'
inputs:
  TEST_PATH:
    description: 'The path of the dart file that is run'
    required: true
    default: 'core/test/integration/export/main.dart'
runs:
  using: 'node20'
  main: '../index.js'